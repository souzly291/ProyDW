import React from 'react';

interface pageProps {
    
}

const page: React.FC<pageProps> = () => {
    return (
        <div>
            Esto es un Login
        </div>
    );
};

export default page;