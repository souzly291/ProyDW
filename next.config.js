/** @type {import('next').NextConfig} */
const nextConfig = {
    // Si no tienes ninguna otra opción experimental,
    // simplemente puedes dejar el objeto nextConfig vacío.
    // Next.js ya sabe que usarás Turbopack al ejecutar:
    // npm run dev --> next dev --turbopack
};

module.exports = nextConfig;

